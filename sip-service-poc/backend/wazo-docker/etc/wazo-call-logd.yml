debug: true
rest_api:
  listen: 0.0.0.0

db_uri: postgresql://asterisk:changeme@postgres/wazo
cel_db_uri: postgresql://asterisk:changeme@postgres/wazo

celery:
  broker: amqp://guest:guest@rabbitmq:5672

bus:
  host: rabbitmq

auth:
  host: auth

confd:
  host: confd

smtp:
  host: NOT-IMPLEMENTED
